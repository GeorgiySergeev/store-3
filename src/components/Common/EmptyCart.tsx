// Find the SVG element in your EmptyCart component and change:
// from: fillRule="evenodd"
// to:   fillRule="evenodd"

// #Similarly, if you have clipRule attributes, change them to clipRule

// For example:
<svg>
  <path fillRule="evenodd" clipRule="evenodd" d="..." />
</svg>;
